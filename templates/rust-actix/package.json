{
	"scripts": {
		"build-api": "cargo build --release",
		"run-api": "cargo run --bin {{project_name}}",
		"build-frontend": "tsc && vite build",
		"dev": "cargo watch -x \"run --bin {{project_name}}\" -C target/dev-env -N --why -w ../../src-actix"
	}
}
